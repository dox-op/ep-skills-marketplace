# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp

# Claude local state (non versionare lo stato locale del plugin manager)
.claude/settings.local.json

# Stato locale di Claude finito nel repo per artefatti d'ambiente (HOME/cache)
plugin/
